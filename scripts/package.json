{
  "name": "openblock-registry-scripts",
  "version": "1.0.0",
  "description": "Scripts for managing OpenBlock Registry",
  "type": "module",
  "scripts": {
    "sync:toolchains": "node toolchains/sync.js",
    "diff:toolchains": "node toolchains/calculate-diff.js",
    "generate:toolchains-md": "node generate-toolchains-md.js",
    "validate": "node validate/index.js",
    "lint": "eslint . --ext .js --fix"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.500.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "archiver": "^6.0.1",
    "chalk": "^5.3.0",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^5.16.0",
    "eslint-config-scratch": "^6.0.0"
  }
}
