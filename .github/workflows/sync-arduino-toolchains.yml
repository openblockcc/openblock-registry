name: Sync Arduino Toolchains

on:
  # Manual trigger
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run - only show what would be done'
        type: boolean
        default: false

  # Weekly schedule - every Monday at 00:00 UTC
  schedule:
    - cron: '0 0 * * 1'

  # Trigger on toolchains.json changes
  push:
    paths:
      - 'toolchains.json'
    branches:
      - main

env:
  R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
  R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
  R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
  R2_BUCKET: ${{ secrets.R2_BUCKET }}
  R2_PUBLIC_URL: ${{ secrets.R2_PUBLIC_URL }}

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: scripts
        run: npm install

      - name: Calculate diff (dry run)
        if: ${{ inputs.dry_run }}
        working-directory: scripts
        run: |
          node toolchains/sync.js --dry-run 2>&1 | tee sync-output.txt

          # Extract markdown report (everything after "## Toolchain Sync Report")
          if grep -q "## Toolchain Sync Report" sync-output.txt; then
            sed -n '/## Toolchain Sync Report/,$p' sync-output.txt >> $GITHUB_STEP_SUMMARY
          else
            echo "## Dry Run Results" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            cat sync-output.txt >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Sync toolchains
        if: ${{ !inputs.dry_run }}
        working-directory: scripts
        run: |
          node toolchains/sync.js 2>&1 | tee sync-output.txt

          # Extract markdown report (everything after "## Toolchain Sync Report")
          if grep -q "## Toolchain Sync Report" sync-output.txt; then
            sed -n '/## Toolchain Sync Report/,$p' sync-output.txt >> $GITHUB_STEP_SUMMARY
          else
            echo "## Sync Results" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            cat sync-output.txt >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Commit and push packages.json
        if: ${{ !inputs.dry_run }}
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add packages.json
          git diff --staged --quiet || git commit -m "chore: sync arduino toolchains"
          git push
